using UnityEngine;public class CameraController : MonoBehaviour{    [SerializeField] private Camera camera1 = null;    [SerializeField] private Camera camera2 = null;    [SerializeField] private float x = 0;    private void Start()    {        //if (Localization.instance != null)        //{            //Localization.instance.currentLanguage = "Russian";        //}    }  private void Update()  {    //camera1.rect = new Rect(x, 0.62f, 0.92f, 0.25f);    //camera2.rect = new Rect(x+1, 0.62f, 0.92f, 0.25f);    //  x -= Time.deltaTime*0.2f;    //  if (x < -1)    //  x = 0;  }}